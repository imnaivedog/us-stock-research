# V5+1 修补包复盘（2026-05-01）

## 背景

V5.7 cutover 期间发现 5 个阻塞 schema migrate / daily ETL 的代码与流程问题。Cutover 用手动兜底命令绕过，V5+1 把这些兜底固化为 repo 内的长期修复。

## Patch 速查

| Patch | Commit | 永久修复 | ADR |
| --- | --- | --- | --- |
| P1 | `a4e191b` | `usstock_data.db` import 时自动加载 repo root `.env`，CLI 不再依赖 `source .env`。 | ADR-026 |
| P2 | `5f29d66` | `DATABASE_URL` 支持 `postgres://` / `postgresql://`，engine 创建前统一 normalize 到 `postgresql+psycopg://`。 | ADR-027 |
| P3 | `7d493d5` | `ddl.sql` 重排为 CREATE TABLE -> ALTER TABLE -> CREATE INDEX，旧表先补列再建依赖索引。 | ADR-027 |
| P4 | `836c4ea` | `corporate_actions` / `fundamentals` expected skip 降噪为 DEBUG + INFO 汇总，transient/provider outage 保留 ERROR。 | ADR-026 |
| P5 | `9711985` | `.gitignore` 增加 `*.bak.*` / `*.bak` / `*.bak.[0-9]*`。 | ADR-026 |
| P6 | `7869de2` | 补 `CHANGELOG.md` 和本复盘文档。 | ADR-026 |
| Follow-up | `68c2903` | `usstock-data` 的 `DATABASE_URL` 缺 password 时从 `POSTGRES_PASSWORD` 补齐，修 LightOS cutover 中 `fe_sendauth: no password supplied`。 | ADR-027 |
| Follow-up | `1402715` | analytics / reports DB helper 同步 `.env` 加载、URL normalize、password fallback，修三层 CLI 行为不一致。 | ADR-027 |
| Follow-up | 本 commit | 收口本地噪音：忽略根 `logs/`，固定 pytest `basetemp`，同步 cutover 文档的 `main` / M-pool command / Notion 字段契约。 | ADR-026 |

## V5+1 vs cutover 兜底

| 问题 | cutover 兜底 | V5+1 永久修 |
| --- | --- | --- |
| CLI 不读 `.env` | 手动 `source .env` | P1 `db.py` 自动 `load_dotenv()` |
| `postgresql://` 走 SQLAlchemy 默认 v2 driver | 手动改前缀 | P2 `_normalize_db_url()` |
| `DATABASE_URL` 缺 password 但 `POSTGRES_PASSWORD` 已有 | 手动改 `.env` 或 export 完整 URL | follow-up 三层 DB helper 运行时补 password |
| data / analytics / reports DB helper 行为不一致 | 逐命令手动 export 完整 URL | follow-up 对齐三层 DB helper |
| 根 `logs/` 目录显示为 untracked | 人工忽略 | follow-up `.gitignore` 增加 `logs/` |
| Windows pytest 结束后 temp 清理偶发 `PermissionError` | 人工确认 tests 已 passed | follow-up 固定 `.pytest_tmp`，降低系统 temp 清理噪音 |
| cutover 文档仍有 `origin master` / `--pool m` | 人工改命令 | follow-up 文档改为 `origin main`，M-pool signals 不带 `--pool` |
| Notion daily DB 字段名不明 | 人工排查 Notion database schema | follow-up 文档补精确 property/type contract |
| 旧表缺列时先建 index 导致 migrate 失败 | 手动 preflight `ALTER TABLE` | P3 DDL 三阶段顺序 |
| corp/fund expected skip 打 ERROR 海 | 人工忽略 | P4 DEBUG skip + INFO 汇总 |
| `.bak.*` 备份文件可能入 commit | 人工检查 | P5 `.gitignore` 通配 |

## 测试覆盖

- P1-P5 每个 patch 均在 Win sandbox 跑过 data / analytics / reports pytest 与 ruff。
- P5 完成时基线：`usstock-data` 36 passed，`usstock-analytics` 30 passed，`usstock-reports` 13 passed，`ruff check` 全绿。
- P6 为文档归档 patch，同样在 push 前跑完整测试矩阵。
- 本地收口后基线：`usstock-data` 39 passed，`usstock-analytics` 33 passed，`usstock-reports` 16 passed，`ruff check` 全绿。

## Provider 域名核查

P4 已 grep `polygon.io` / `api.polygon`。当前只有 `packages/usstock-data/src/usstock_data/etl/shares_outstanding.py` 使用 `https://api.polygon.io`。API provider/domain 切换属于用户确认事项，本修补包不切换到 Massive。

## 后续（不在 V5+1）

- 用户在 LightOS 跑 P6 后最终验收与 cutover rerun。
- A 池 thesis 数字和业务文字由用户维护。
- Hermes MCP client-side 接入、Notion 同步、GCP 旧资源删除、secrets 轮换均为用户保留事项。
